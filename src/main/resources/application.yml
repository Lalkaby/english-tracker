---
  server:
    port: 9120

  spring:
    datasource:
      url: jdbc:${DATABASE_JDBC_URL}
      username: ${DATABASE_USERNAME}
      password: ${DATABASE_PASSWORD}
      driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true